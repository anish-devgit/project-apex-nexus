[package]
name = "nexus_binding"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.14", features = ["tokio_rt"] }
napi-derive = "2.14"
nexus_core = { path = "../nexus_core" }

[build-dependencies]
napi-build = "2"
