# Contributing to Nexus

We welcome contributions!

## Development Setup

1. Install Rust and Node.js.
2. Clone the repo.
3. Run `cargo build`.
4. Run `pnpm install` in `packages/nexus-cli`.

## Guidelines

- Use `cargo fmt` for Rust code.
- Follow the "Rust Kernel, Node.js Shell" philosophy.
